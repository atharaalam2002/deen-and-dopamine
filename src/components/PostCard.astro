---
const { href, title, excerpt, pubDate, tags=[] } = Astro.props;
---
<a href={href} class="group p-6 rounded-2xl bg-slate-900/50 ring-1 ring-white/10 hover:ring-brand-500/40 hover:shadow-glow block">
  <h3 class="font-semibold group-hover:text-brand-200">{title}</h3>
  <p class="mt-2 text-sm text-slate-400">{excerpt}</p>
  <div class="mt-3 flex items-center gap-2 text-[11px] text-slate-400">
    <span>{new Date(pubDate).toLocaleDateString()}</span>
    <span>•</span>
    <div class="flex gap-2">{tags.map(t => <span class="px-2 py-0.5 rounded bg-slate-800/70">{t}</span>)}</div>
  </div>
</a>
